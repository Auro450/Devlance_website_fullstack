generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Lead {
  id           String   @id @default(cuid())
  name         String
  phone        String
  email        String?
  businessName String
  businessType String
  requirements String
  createdAt    DateTime @default(now())
}
